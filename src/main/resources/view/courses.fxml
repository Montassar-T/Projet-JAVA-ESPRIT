<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx/21"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="tn.esprit.educlass.controller.CourseController"
            prefWidth="800" prefHeight="500">

    <!-- TOP SEARCH / FILTER BAR -->
    <top>
        <VBox spacing="10">
            <padding>
                <Insets top="12" right="12" bottom="12" left="12"/>
            </padding>

            <HBox spacing="10" alignment="CENTER_LEFT">

                <TextField fx:id="searchField"
                           promptText="Rechercher des cours..."
                           HBox.hgrow="ALWAYS"/>

                <Button text="Rechercher"
                        fx:id="searchBtn"
                        onAction="#onSearch"/>

                <Label text="Niveau:"/>
                <ComboBox fx:id="levelFilter" prefWidth="130" promptText="Choisir un niveau"/>

                <Button text="Appliquer"
                        fx:id="applyFilterBtn"
                        onAction="#onApplyFilters"/>

                <Button text="Réinitialiser"
                        fx:id="resetFilterBtn"
                        onAction="#onResetFilters"/>

            </HBox>
        </VBox>
    </top>

    <!-- GRID CONTENT -->
    <center>
        <ScrollPane fitToWidth="true" pannable="true">
            <content>
                <GridPane fx:id="courseGrid"
                          hgap="30"
                          vgap="30">
                    <padding>
                        <Insets top="20" right="20" bottom="20" left="20"/>
                    </padding>
                </GridPane>
            </content>
        </ScrollPane>
    </center>

</BorderPane>
